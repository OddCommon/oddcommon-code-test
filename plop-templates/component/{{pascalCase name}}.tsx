import React from 'react';

interface {{pascalCase name}}Props {
    children: React.ReactNode;
}

const {{ pascalCase name }} = ({children}: {{pascalCase name}}Props): JSX.Element => {
    return (
     <div>
        {children}
     </div>
    );
  };
  
  export default {{ pascalCase name}};